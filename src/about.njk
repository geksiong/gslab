---
layout: default
title: About Me
---

<div class="flex flex-col items-center text-lg leading-relaxed">
    {% if author.avatar %}
    <a href="{{ author.homepage }}" target="_blank" rel="noopener">
        <img class="h-32 rounded-full md:h-40" alt="Author avatar" src="{{ author.avatar }}">
    </a>
    {% endif %}
    <div class="mt-8 text-center">
        <h1 class="mt-0 mb-1 text-2xl">
            <a href="{{ author.homepage }}" target="_blank" rel="noopener">{{ author.name }}</a>
        </h1>
        {% if author.email %}
        <div class="text-gray-600">
            <a href="mailto:{{ author.email }}">{{ author.email }}</a>
        </div>
        {% endif %}
        {% if author.phone %}
        <div class="text-gray-600">
            <a href="tel:{{ author.phone }}">{{ author.phone }}</a>
        </div>
        {% endif %}
        {% if author.bio %}
        <p class="mt-3 text-gray-700">{{ author.bio }}</p>
        {% endif %}
    </div>
</div>
